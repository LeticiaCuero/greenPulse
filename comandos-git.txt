# Puxar as alterações remotas com rebase
git pull --rebase origin main

# Se houver conflitos, resolva-os e depois continue o rebase
# Após resolver os conflitos (se houver):
git add .
git rebase --continue

# Quando o rebase estiver completo, faça o push
git push origin main
